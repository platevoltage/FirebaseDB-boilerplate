import { useState, useRef, useEffect } from 'react';

interface Props {
    job: any;
}

export default function Invoice({job}: Props) {
    return (
        <div>
            Invoice
        </div>
    );
}
